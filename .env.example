# SQLite database file (relative to project root).
# On localhost, use a relative path so it works regardless of where you clone.
DATABASE_URL="file:./db/custom.db"

# Optional: override ffmpeg/yt-dlp binary locations if not on PATH.
# FFMPEG_BIN=/usr/local/bin/ffmpeg
